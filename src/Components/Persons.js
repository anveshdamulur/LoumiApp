
import React from 'react'
export default function Persons(props) {
    return (
        <div>
            name : {props.person.name}
            {/* image : <img src={props.person.image} alt=""/> */}
            
        </div>
    )
}
